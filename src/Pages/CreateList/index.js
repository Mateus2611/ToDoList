import React from "react";
import { Text, View } from "react-native";
import { StyledComponent } from "nativewind";

export default function CreateList({ navigation }) {
	return (
		<View>
			<Text>Created List</Text>
		</View>
	);
}
